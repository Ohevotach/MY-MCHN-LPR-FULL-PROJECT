# YOLO dataset config for character-box detection on rectified plate images.
# Use ordinary YOLO labels. The detector only finds character boxes;
# Modern Hopfield still recognizes the character class.
path: ./dataset/yolo_chars
train: images/train
val: images/val
test: images/val

names:
  0: plate_char
